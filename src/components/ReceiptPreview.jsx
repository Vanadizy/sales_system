import { money } from '../utils/format'
import { Brand } from './UI'

export default function ReceiptPreview({ company, branch, items, subtotal, discount, tax, total, payment }) {
  return <div className="rounded-2xl border bg-white p-5 text-slate-900 dark:bg-slate-50"><div className="mb-5 text-center"><div className="flex justify-center"><Brand company={company} compact /></div><p className="mt-2 text-sm font-bold">{company?.name}</p><p className="text-xs text-slate-500">{branch?.name}</p></div><div className="border-y border-dashed py-3 text-sm">{items.length ? items.map((item) => <div className="mb-2 flex justify-between" key={item.productId}><span>{item.qty} x {item.name}</span><span>{money(item.price * item.qty, company?.currency)}</span></div>) : <p className="py-4 text-center text-xs text-slate-400">Cart items appear here</p>}</div><div className="space-y-2 py-3 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{money(subtotal, company?.currency)}</span></div><div className="flex justify-between"><span>Discount</span><span>- {money(discount, company?.currency)}</span></div>{tax > 0 && <div className="flex justify-between"><span>Tax</span><span>{money(tax, company?.currency)}</span></div>}<div className="flex justify-between border-t pt-2 text-base font-bold"><span>Total</span><span>{money(total, company?.currency)}</span></div></div><p className="mt-2 text-center text-xs text-slate-500">Payment: {payment}<br />{company?.receiptFooter}</p></div>
}
